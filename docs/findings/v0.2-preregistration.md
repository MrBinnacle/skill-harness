# v0.2 pre-registration — Full-vs-Null skill evaluation (DRAFT)

**Status:** DRAFT, filled 2026-07-09 against the amended entry gate
([v0.2-reaim-gate.md](v0.2-reaim-gate.md)). The tracer-bullet build may proceed on this
draft; **no sized paid run starts until the MDE/sizing and stopping-rule fields are
finalized from tracer-measured noise** and this header flips to REGISTERED. Amendments
after REGISTERED require a dated correction block, never an edit.

## Gate fields

| Field | Value |
|---|---|
| Pending decision | Which skills in the operator's live Claude Code setup (~75 skills) earn their slot — per-skill keep/cut, the recurring Gate-2 decision this harness exists to serve. First subject skill: one from that live library, chosen for having a real task domain with mechanically checkable outcomes. |
| Primary contrast | Full vs Null, whole-skill, paired per task. Full = agent runs with the skill installed; Null = identical agent, config, and task without it. One primary test per skill (multiplicity family = 1). **Portfolio note (2026-07-09, DRAFT-stage):** the eventual library-wide keep/cut sweep (~75 skills) is a family of ~75 primary tests — the REGISTERED text must declare its portfolio multiplicity treatment (Benjamini-Hochberg FDR or hierarchical shrinkage) before that sweep launches; the single-skill tracer is exempt (family = 1). **Null-arm definition (POC-established 2026-07-09):** Null is the STOCK agent environment — Claude Code's built-in skills remain present in both arms; the arms differ by exactly the one skill under test. The contrast therefore answers "does adding this skill to a normal setup change outcomes," not "skills vs no skills." |
| Task source | Real tasks from the skill's actual domain, sampled from the operator's genuine workload — NOT tasks selected because the skill addresses them (findings doc §3). Task list frozen before any Full/Null assignment. |
| Oracle set | Tracer bullet: `tests-pass` (deterministic, transcript-derived) as the single Tier-1 outcome oracle. Full run adds: lint delta, diff size, turn count, cost. Judge-graded axes only via the v0.1 calibration machinery (calibrated (judge, axis) pairs), else UNMEASURED. |
| Subject layer | Inspect (`inspect_ai`) + `inspect_swe.claude_code()` as the agentic subject. Full/Null realized via the agent's first-class `skills=` parameter. Sandbox: Docker. POC verified END-TO-END 2026-07-09 (inspect-ai 0.3.245, inspect-swe 0.2.65, Windows host, Debian bookworm sandbox, Claude Code agent, OpenRouter-bridged model): (a) agent completes a file-creation task scored from sandbox state, accuracy 1.0; (b) paired Full/Null via `skills=` — Full arm's agent enumerates the injected skill, Null arm's does not, 1.0/1.0. Operational facts: host venv needs `openai` AND `anthropic` (bridge deps, errors are silent-ish without debug=True); outcome oracles MUST resolve paths against the agent's pinned `cwd` (default `/root`), never the sandbox default. Fallback if the sandboxed path develops ≥1 session of friction: the isolated-config-root local runner from the source research arc. |
| Harness pin | Recorded per trial, identical across arms, or the trial is inadmissible: inspect-ai version, inspect-swe version, `claude_code(version=)` pinned to an exact agent version (never `auto`), model id, sandbox image digest, `env`, `disallowed_tools`. Rationale: harness-induced variance on identical weights reaches 10–20pp — larger than most skill effects. **Implementation status (2026-07-09, DRAFT-stage note):** the live `HarnessPin` records versions/agent/model/sandbox-type/`cwd`; the write path (`skill_harness.subject.ingest`) enforces cross-arm fingerprint equality at write time and stamps the full pin JSON on every sample. **Update (same day):** the pin extension is DELIVERED — `HarnessPin` now carries `sandbox_image` (floating tags resolved to `@sha256` digests via live Docker at capture, refusing when unresolvable), `env`, and `disallowed_tools` (sorted for canonical fingerprinting), and `build_paired_tasks` enforces them by injection: both arms run a digest-pinned compose file with env/disallowed threaded from the same pin. Digest resolution verified against a live Docker daemon. This row is now fully implementable as written. |
| Differentiation vs field | Checked 2026-07-09. Anthropic prescribes Full-vs-Null but ships no runner; `adewale/skill-eval-harness` ships paired lift but with uncalibrated user-supplied judges and flags (not refusals) on weak evidence. This run produces: admissibility-gated verdicts in the append-only evidence store with UNMEASURED as a first-class outcome, and harness-pin admissibility no existing tool enforces. Interop, not rebuild: the subject layer IS Inspect. |
| MDE + sizing | ANALYTIC HALF DONE (2026-07-09), EMPIRICAL HALF OPEN. For the binary `tests-pass` oracle the sizing is exact, not simulated: `skill_harness.ablation.sizing` solves the locked stopping rule by dynamic programming over the generative pair (d = per-epoch discordance rate, q = P(Full wins \| discordant)). Results: null (q=0.5) false-PASS ≤ 1.8% at any d; minimum detectable q at 80% power = 0.77 (d=1.0), 0.83 (d=0.8), 0.94 (d=0.6); at d ≲ 0.5 NO effect up to q=1.0 is detectable inside N_max=40 — the contrast is structurally UNMEASURED. Expected cost per (skill, task): E[N] 8–40 paired epochs, ≈$2.40–$12 at the POC-observed ~$0.30/pair. CONSEQUENCE: the noise micro-run's primary estimand is the DISCORDANCE RATE d (not CV — ties carry no directional signal under the half-update encoding); task selection must guard the ceiling (a task Null always passes drives d→0 and burns N_max for UNMEASURED). CV of continuous secondaries (tokens/cost/turns) is measured alongside. OPEN until the micro-run: measured d on a real (skill, task) pair; the sized run's n × k follows from the table row d selects. |
| Stopping rule | Beta-Binomial sequential bounds (`ablation/stopping.py`), N_max stated at sizing time. Tracer bullet: fixed micro-k, exploratory-only, no verdict claims. |
| Leak audit | Before any paid trial: grep every task artifact visible to the subject for (a) the expected fix/answer strings, (b) the skill's name and distinctive vocabulary. Failing-test files stripped to assertion-minimum — no docstrings stating the fix (oracle-authoring rule, findings doc §4). Post-run: transcript grep confirming the subject never read an answer-bearing artifact. |
| Budget | Tracer bullet ≤ $10 total, per-trial cap enforced by the existing cost-ledger discipline. Sized run budgeted at sizing time; hard tripwire recorded here before launch. |

## Tracer-bullet scope (the build this draft licenses)

One skill, one task, Full + Null, k small, `tests-pass` oracle, harness pin recorded,
result written to the evidence store through the existing admissibility machinery. The
deliverable is the working end-to-end path and a noise estimate — explicitly NOT a
keep/cut verdict on the subject skill.

**First paired shot (2026-07-09, via `skill_harness.subject.build_paired_tasks`,
k=1, ~$0.30):** apparatus works — identical pin fingerprint stamped on both arms,
Full 1.0 / Null 0.0. The differential is an ARTIFACT, transcript-audited: the Null
arm's agent completed the task but wrote to `/answer.txt` instead of the pinned cwd
(`/root/answer.txt`) — "current working directory" in a prompt is model-interpretable.
A naive k=1 reading would have reported the skill as +1.0 lift. **Protocol rule
adopted:** task prompts for file-based oracles must state ABSOLUTE paths; the oracle
and the prompt must name the same literal path. This is findings-doc §1 (noise ≫
k=1 differentials) reproducing on the first live pair.

**Write path delivered (2026-07-09, `skill_harness.subject.ingest`):** paired
`.eval` logs now flow into the append-only evidence store — run + per-(arm, epoch)
samples (with pin JSON + fingerprint, migration 0500) + per-epoch Tier-1
`full_vs_null` outcome verdicts, harness-pin admissibility resolved at write time,
deterministic run id refusing re-ingest. Verified end-to-end against this first
paired shot's logs. Remaining before the sized run: pin extension (see Harness pin
row), noise micro-run, MDE/sizing, DRAFT→REGISTERED flip.
