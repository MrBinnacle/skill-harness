# v0.2 pre-registration — Full-vs-Null skill evaluation (REGISTERED)

> **Reader's note (added 2026-07-10; non-substantive, alters no registered content).** What
> this document is: the v0.2 measurement plan, locked *before* the data was collected — what
> will be tested, how, and what counts as a result — so the outcome can't quietly reshape the
> method. "Pre-registration" is the practice, borrowed from experimental science; everything
> below the status line is the registered record and changes only by dated correction blocks.

**Status:** REGISTERED 2026-07-10. Filled 2026-07-09 against the amended entry gate
([v0.2-reaim-gate.md](v0.2-reaim-gate.md)) as DRAFT; flipped to REGISTERED after the
noise micro-run completed (results section below) and the MDE/sizing row was finalized
from measured data. Amendments after this point require a dated correction block,
never an edit. **Registered bottom line:** the micro-run returned its pre-stated NO-GO
— no sized paid run launches under this registration until the task-sourcing condition
in the results section is met. The stopping rule's locked constants stand, and the
detectability bar they imply is stated plainly: this instrument detects TRANSFORMATIVE
skills only (operator-accepted 2026-07-09; see Detectability disclosure).

## Gate fields

| Field | Value |
|---|---|
| Pending decision | Which skills in the operator's live Claude Code setup (~75 skills) earn their slot — per-skill keep/cut, the recurring Gate-2 decision this harness exists to serve. First subject skill: one from that live library, chosen for having a real task domain with mechanically checkable outcomes. |
| Primary contrast | Full vs Null, whole-skill, paired per task. Full = agent runs with the skill installed; Null = identical agent, config, and task without it. One primary test per skill (multiplicity family = 1). **Portfolio note (2026-07-09, DRAFT-stage):** the eventual library-wide keep/cut sweep (~75 skills) is a family of ~75 primary tests — the REGISTERED text must declare its portfolio multiplicity treatment (Benjamini-Hochberg FDR or hierarchical shrinkage) before that sweep launches; the single-skill tracer is exempt (family = 1). **Null-arm definition (POC-established 2026-07-09):** Null is the STOCK agent environment — Claude Code's built-in skills remain present in both arms; the arms differ by exactly the one skill under test. The contrast therefore answers "does adding this skill to a normal setup change outcomes," not "skills vs no skills." |
| Task source | Real tasks from the skill's actual domain, sampled from the operator's genuine workload — NOT tasks selected because the skill addresses them (findings doc §3). Task list frozen before any Full/Null assignment. |
| Oracle set | Tracer bullet: `tests-pass` (deterministic, transcript-derived) as the single Tier-1 outcome oracle. Full run adds: lint delta, diff size, turn count, cost. Judge-graded axes only via the v0.1 calibration machinery (calibrated (judge, axis) pairs), else UNMEASURED. |
| Subject layer | Inspect (`inspect_ai`) + `inspect_swe.claude_code()` as the agentic subject. Full/Null realized via the agent's first-class `skills=` parameter. Sandbox: Docker. POC verified END-TO-END 2026-07-09 (inspect-ai 0.3.245, inspect-swe 0.2.65, Windows host, Debian bookworm sandbox, Claude Code agent, OpenRouter-bridged model): (a) agent completes a file-creation task scored from sandbox state, accuracy 1.0; (b) paired Full/Null via `skills=` — Full arm's agent enumerates the injected skill, Null arm's does not, 1.0/1.0. Operational facts: host venv needs `openai` AND `anthropic` (bridge deps, errors are silent-ish without debug=True); outcome oracles MUST resolve paths against the agent's pinned `cwd` (default `/root`), never the sandbox default. Fallback if the sandboxed path develops ≥1 session of friction: the isolated-config-root local runner from the source research arc. |
| Harness pin | Recorded per trial, identical across arms, or the trial is inadmissible: inspect-ai version, inspect-swe version, `claude_code(version=)` pinned to an exact agent version (never `auto`), model id, sandbox image digest, `env`, `disallowed_tools`. Rationale: harness-induced variance on identical weights reaches 10–20pp — larger than most skill effects. **Implementation status (2026-07-09, DRAFT-stage note):** the live `HarnessPin` records versions/agent/model/sandbox-type/`cwd`; the write path (`skill_harness.subject.ingest`) enforces cross-arm fingerprint equality at write time and stamps the full pin JSON on every sample. **Update (same day):** the pin extension is DELIVERED — `HarnessPin` now carries `sandbox_image` (floating tags resolved to `@sha256` digests via live Docker at capture, refusing when unresolvable), `env`, and `disallowed_tools` (sorted for canonical fingerprinting), and `build_paired_tasks` enforces them by injection: both arms run a digest-pinned compose file with env/disallowed threaded from the same pin. Digest resolution verified against a live Docker daemon. This row is now fully implementable as written. |
| Differentiation vs field | Checked 2026-07-09. Anthropic prescribes Full-vs-Null but ships no runner; `adewale/skill-eval-harness` ships paired lift but with uncalibrated user-supplied judges and flags (not refusals) on weak evidence. This run produces: admissibility-gated verdicts in the append-only evidence store with UNMEASURED as a first-class outcome, and harness-pin admissibility no existing tool enforces. Interop, not rebuild: the subject layer IS Inspect. |
| MDE + sizing | ANALYTIC HALF DONE (2026-07-09), EMPIRICAL HALF OPEN. For the binary `tests-pass` oracle the sizing is exact, not simulated: `skill_harness.ablation.sizing` solves the locked stopping rule by dynamic programming over the generative pair (d = per-epoch discordance rate, q = P(Full wins \| discordant)). Results: null (q=0.5) false-PASS ≤ 1.8% at any d; minimum detectable q at 80% power = 0.77 (d=1.0), 0.83 (d=0.8), 0.94 (d=0.6); at d ≲ 0.5 NO effect up to q=1.0 is detectable inside N_max=40 — the contrast is structurally UNMEASURED. Expected cost per (skill, task): E[N] 8–40 paired epochs, ≈$2.40–$12 at the POC-observed ~$0.30/pair. CONSEQUENCE: the noise micro-run's primary estimand is the DISCORDANCE RATE d (not CV — ties carry no directional signal under the half-update encoding); task selection must guard the ceiling (a task Null always passes drives d→0 and burns N_max for UNMEASURED). CV of continuous secondaries (tokens/cost/turns) is measured alongside. **EMPIRICAL HALF DONE (2026-07-10, Stage 1 paired k=8, task v2):** x = 0/8 discordant epochs — d̂ = 0.00, Jeffreys 95% CI **[0.00, 0.26]**. Registered as an interval per the analysis rules; even the interval's UPPER bound sits in the sizing table's structurally-UNMEASURED region (d ≲ 0.5 → no effect detectable inside N_max=40 at any q). Honest sizing conclusion: on this task class no sized run can be sized at all — see the results section's task-sourcing condition. Measured per-pair cost on a real task: ≈$0.77/pair token-computed (not the POC's $0.30), so E[cost] for a hypothetical sized run is ≈$6–$31 per (skill, task). |
| Stopping rule | Beta-Binomial sequential bounds (`ablation/stopping.py`), N_max stated at sizing time. Tracer bullet: fixed micro-k, exploratory-only, no verdict claims. |
| Leak audit | Before any paid trial: grep every task artifact visible to the subject for (a) the expected fix/answer strings, (b) the skill's name and distinctive vocabulary. Failing-test files stripped to assertion-minimum — no docstrings stating the fix (oracle-authoring rule, findings doc §4). Post-run: transcript grep confirming the subject never read an answer-bearing artifact. |
| Budget | Tracer bullet ≤ $10 total, per-trial cap enforced by the existing cost-ledger discipline. Sized run budgeted at sizing time; hard tripwire recorded here before launch. |

## Tracer-bullet scope (the build this draft licenses)

One skill, one task, Full + Null, k small, `tests-pass` oracle, harness pin recorded,
result written to the evidence store through the existing admissibility machinery. The
deliverable is the working end-to-end path and a noise estimate — explicitly NOT a
keep/cut verdict on the subject skill.

**First paired shot (2026-07-09, via `skill_harness.subject.build_paired_tasks`,
k=1, ~$0.30):** apparatus works — identical pin fingerprint stamped on both arms,
Full 1.0 / Null 0.0. The differential is an ARTIFACT, transcript-audited: the Null
arm's agent completed the task but wrote to `/answer.txt` instead of the pinned cwd
(`/root/answer.txt`) — "current working directory" in a prompt is model-interpretable.
A naive k=1 reading would have reported the skill as +1.0 lift. **Protocol rule
adopted:** task prompts for file-based oracles must state ABSOLUTE paths; the oracle
and the prompt must name the same literal path. This is findings-doc §1 (noise ≫
k=1 differentials) reproducing on the first live pair.

## Noise micro-run protocol (v1, 2026-07-09 — research-informed + pre-mortemed)

Replaces the informal "k=8, discordance primary" sketch after a targeted best-practices
sweep and an adversarial pre-mortem. Prior art (verified against sources): run-to-run
variance in agentic SWE evals is large and real (arXiv:2602.07150 — 60k SWE-bench-Verified
trajectories, single-run pass@1 varies 2.2–6.0pp, SD >1.5pp at temperature 0); repetition
counts of n=8–16 suffice for reliability estimates on structured tasks (arXiv:2512.06710);
and measuring the paired-correlation nuisance BEFORE sizing a paired test is exactly what
the paired-eval literature prescribes (arXiv:2605.30315 — unpaired sizing shortcuts are
~2× wrong in close comparisons). No published cross-arm discordance-rate protocol exists;
this one is registered here.

**Stage 0 — Null-only screen + cost calibration (≈$1–2, screen data never enters verdicts):**
3 Null-arm epochs on the candidate task. Kills three pre-mortem risks at once:
ceiling check (Null 3/3 pass → reject task, try next candidate), REAL per-epoch cost
(the $0.30/pair figure is anchored on a trivial k=1 file-creation POC — a genuine task
may cost 3–10×), and apparatus shakedown before paired spend.

**Stage 1 — Paired run:** k = 16 paired epochs if Stage-0 cost permits within budget,
else k = 8 floor. Hard cap $10 total including Stage 0. Launch config (each item traces
to a named pre-mortem failure mode): direct Anthropic API with pinned model id — NOT
OpenRouter (router backend heterogeneity inflates measured discordance; bridge reports
no cost, blinding the budget cap); `max_sandboxes` ≈ 3 (default would launch all
containers at once on Docker Desktop); `retry_on_error=1` (one errored epoch otherwise
voids the whole ingest — the write path refuses unpaired epoch sets); agent version
pinned at launch AND captured in-sandbox (`claude --version` into sample metadata —
a launch-parameter pin is a claim, not a measurement); oracle test files hash-checked
or re-copied pristine at scoring time (tests-pass is agent-tamperable in the sandbox);
leak audit = static grep + a fresh-context reviewer pass over every subject-visible
artifact + the registered post-run transcript grep (the known oracle-self-leak class
defeats grep alone).

**Analysis rules (pre-stated, before any data):**
- d is registered as a Jeffreys 95% INTERVAL, never a point estimate — at k=8–16 the
  interval spans more than one sizing-table row and pretending otherwise writes noise into
  the REGISTERED field dressed as measurement.
- GO/NO-GO for the sized run: GO iff observed discordant epochs x ≥ ⌈0.5625·k⌉
  (k=16 → 9; k=8 → 5). Operating characteristic (exact binomial): P(GO | d=0.3) ≤ 6%,
  P(GO | d=0.8) ≥ 94%.
- Sizing uses the ~25th percentile of the d posterior, and treats even that as an
  UPPER BOUND on task-population d: the ceiling-guard task selection conditions on
  Null failures (selection toward high-discordance tasks). The sized run must restate
  this and size for a task MIXTURE (absorption is convex in d — sizing at the mean d
  is optimistic).
- Micro-run report template contains: d interval, discordance count, CV of
  tokens/turns/cost, apparatus notes. It contains NO win-direction field — k=8–16 is
  exactly the stopping rule's first-check region, and a "Full won 7/8, exploratory-only"
  sentence in project state would steer the REGISTERED thresholds it exists to set.
- Epoch independence basis (verified in inspect_ai 0.3.245 source): fresh sandbox per
  (sample, epoch); model-response cache is epoch-keyed by default. Residual: provider-side
  prompt-cache warm/cold differences load onto epoch-1-vs-rest cost/latency, so cost CV
  is read with that caveat.

**Detectability disclosure (RESOLVED at registration — operator-accepted values
decision, 2026-07-09):** under arm-independence the locked rule's 80%-power region
requires roughly Null pass ≤ ~0.3 AND Full pass ≥ ~0.8 — i.e., it detects
TRANSFORMATIVE skills only, and its FAIL/cut verdict fires readily on no-effect
skills. The operator explicitly accepted "keep only if transformative" as the keep/cut
bar; the locked constants stand unchanged. Any future re-opening of N_max/threshold
requires a dated correction block here first.

**Write path delivered (2026-07-09, `skill_harness.subject.ingest`):** paired
`.eval` logs now flow into the append-only evidence store — run + per-(arm, epoch)
samples (with pin JSON + fingerprint, migration 0500) + per-epoch Tier-1
`full_vs_null` outcome verdicts, harness-pin admissibility resolved at write time,
deterministic run id refusing re-ingest. Verified end-to-end against this first
paired shot's logs. Remaining before the sized run: pin extension (see Harness pin
row), noise micro-run, MDE/sizing, DRAFT→REGISTERED flip.

## Noise micro-run — RESULTS (2026-07-10, the registration record)

Subject: `sqlite-expert` (29-line thin skill, genuine cut-candidate). Task domain:
SQLite FTS5 notes-search module, stdlib-only, scored by a pristine-at-score-time
pytest oracle (base64-materialized at scoring — never subject-visible). Agent:
Claude Code 2.1.197 pinned, model `anthropic/claude-sonnet-5` via direct Anthropic
API, digest-pinned sandbox image, one `HarnessPin` shared by both arms (fingerprint
`2f76c933…`, verified identical on all 16 samples at ingest).

**Stage 0, task v1** (trigger-sync + phrase-escaping + ranking): Null **3/3 PASS** —
ceiling, screen FAILED per protocol; task rejected, iterate harder.

**Task v2** (v1 base + a full boolean query language: quoted phrases, implicit-AND,
OR-precedence, `-`exclusions incl. phrase exclusion, punctuation splits, unbalanced-quote
recovery, title-weighted ranking, 34 asserts): leak-audited per the registered rule
(static grep both directions + fresh-context reviewer, which removed a ranking-API
mention from the prompt that named the gold mechanism; one placebo tie-break assert
hardened into a real reversed-id probe). **Stage 0, task v2: Null 3/3 PASS** — ceiling
AGAIN. Verified from the raw `.eval` logs, not the runner's summary.

**Double-ceiling decision (operator, 2026-07-09):** protocol said stop after a second
failed screen. Reported; the operator approved running Stage 1 k=8 on v2 anyway with
its purpose REFRAMED AND RE-REGISTERED BEFORE LAUNCH as: end-to-end apparatus
shakedown, CV secondaries, Full-arm input-tax measurement, and the d upper bound as
the pre-stated **NO-GO datum** — explicitly not a benefit measurement.

**Stage 1 (paired k=8, one pin both arms, ingested through write-time admissibility):**

| Field (registered template) | Value |
|---|---|
| Paired epochs analyzed | 8 (Full 8/8 success, Null 8/8 success) |
| Discordant epochs x | **0** — d̂ = 0.00, Jeffreys 95% CI **[0.00, 0.26]** |
| GO/NO-GO (pre-stated x≥5) | **NO-GO** |
| Null pass rate p0 (hardness check) | 8/8 — ceiling, consistent with both screens |
| CV out_tokens | Full 0.134 (mean 25,322) · Null 0.413 (mean 29,456) |
| CV in_tokens | Full 0.170 · Null 0.185 |
| Full-arm input tax | cache_write Full 232,668 vs Null 204,646 (+28,022 ≈ the skill payload, re-written per epoch); cache_read Full 1.66M vs Null 1.79M |
| Spend | Stage 1 ≈ **$6.17** token-computed at intro pricing (≈$0.77/pair); Stage 0 screens ≈ $1 combined; run total well under the $10 cap |
| Evidence store | run_id `956aef8f…`, 8 admissible verdicts, all observation 0.5 (tie encoding), re-ingest refused |

No win-direction field, per the analysis rules (x=0: none exists).

**Apparatus notes:** (a) in-sandbox `claude --version` capture soft-failed ("unknown"
— the binary is not on the scorer's PATH at score time; the launch-parameter pin
stands, but the in-sandbox measurement remains a claim until the capture rides the
agent transcript instead); (b) Inspect fell back to estimated token counts
(`count_tokens` unavailable for the model id), so token-computed costs are
estimate-based; (c) the bridge reports `usd: 0` per sample — cost MUST be
token-computed, confirming the FM-4 pre-mortem note; (d) `AF_UNIX` control-surface
warning on Windows hosts is benign; (e) `retry_on_error=1` + `max_sandboxes=3` ran
clean — no retries consumed, no HTTP retries.

**REGISTERED FINDING — the double-ceiling:** stock claude-sonnet-5 with no skill
passed **14/14 Null epochs across two deliberately-hardened task versions** (3+3
screens, 8 Stage-1). On well-specified, self-contained, offline-checkable tasks — the
only class the current leak-audit + sandboxed-oracle machinery admits — the Null arm
sits at ceiling, d → 0, and Full-vs-Null is **structurally UNMEASURED regardless of
budget**. This is a property of the task class × frontier-model capability, not an
apparatus defect (the apparatus shakedown was clean end-to-end).

## AMENDMENT 1 — Batch-1 registration (2026-07-10, dated block per the amendment rule)

A library-disposition audit (4-seat parallel classification of the operator's 72-skill
live library, hub-verified; artifact held in a private research workspace)
produced exactly four EVAL-CONTESTED skills — candidates whose keep/cut genuinely needs
empirical evidence AND whose home domain admits a deterministic oracle. This amendment
registers them as **batch-1** BEFORE any oracle is authored or dollar spent:

| # | Skill | Screen task (sourced from documented origin failure / live workload, per the task-sourcing condition) |
|---|---|---|
| 1 | sqlite-tie-break-red-test-trap | Repo with `ORDER BY ts DESC LIMIT N` lacking a tie-break; agent must author a FAILING regression test; oracle = the test is RED at unfixed HEAD (naive insertion order yields a passing placebo). Origin: observed in a personal production project's placebo test. |
| 2 | bayesian-eval-discipline | Implement PASS/FAIL aggregation, K=20 clauses, P(p>0.6)≥0.95, 30% ties; oracle checks minimum-N floor + explicit multiplicity treatment present. Sourced from this harness's own live aggregation workload. |
| 3 | append-only-evidence-design | Evidence store whose verdict rows stay immutable against raw sqlite3 CLI writes, invariant surviving a table-recreating migration; oracle = trigger-abort + post-migration tamper test. Sourced from this repo's evidence-store domain. |
| 4 | llm-judge-calibration | Design a judge admissibility gate; oracle checks pairwise (not scalar) schema, position-swap double call, calibration floor, prompt-hash-inclusive judge_id. Sourced from the Phase-B judge workload. |

**Pre-stated rules for batch-1:**
- Family = 4 primary tests → **Benjamini-Hochberg FDR at q=0.10 across the four**,
  declared here per the Primary-contrast portfolio note (the single-skill exemption no
  longer applies).
- Protocol = the registered two-stage design unchanged: Stage-0 Null screen (3 epochs,
  ≈$0.50) per task; paired Stage-1 (k=8 floor, x≥5) ONLY where the screen shows
  p0 < 1 — the task-sourcing condition binds. A screen that ceilings is recorded as a
  NO-GO datum for that skill (subsumption evidence under the operator-accepted
  transformative-only bar), not re-iterated more than once per skill.
- Oracle authoring bound by the standing rules: assertion-minimum docstrings (no fix
  text — the C3 self-leak scar), gold GREEN + naive RED validation, static leak grep
  both directions + fresh-context reviewer pass, gold fix quarantined from every
  subject-visible artifact.
- Budget: screens ≈$2 total; paired runs ≈$6–7 per surviving skill at measured
  per-pair cost; hard cap $30 for the whole batch.
- Reproducibility note: the prior subject skill `sqlite-expert` was ARCHIVED from the
  live library on 2026-07-10 (disposition: cut, on this registration's own null result);
  its content persists at the library's `_archive/` for any re-run.

**Task-sourcing condition (gates any sized run under this registration):** a sized
run launches only when the task source produces tasks where Null demonstrably fails
(p0 materially < 1 on a Stage-0 screen) — in practice: real-workload tasks sampled
from the operator's genuine backlog with ecological context, underspecification, or
scale that a self-contained synthetic cannot carry. Alternatively the harness reports
UNMEASURED as the verdict for skills whose domains only present ceiling-class tasks —
which is itself a first-class, publishable outcome of this instrument. Sizing for any
such run must restate the selection-toward-high-d caveat and size for a task mixture
(see analysis rules).

### Batch-1 results (dated blocks; screens per the two-stage protocol)

**2026-07-10 — Skill 1/4, `sqlite-tie-break-red-test-trap`: Stage-0 Null screen
3/3 PASS — CEILING (p0 = 1). NO paired run, per the pre-stated p0 < 1 gate.**
Task as registered (origin: a personal production project's placebo test): fixture repo with
`ORDER BY created_at DESC LIMIT ?` missing its documented id tie-break; agent must
author a regression test that is RED at unfixed HEAD. Oracle (validated 13/13
against gold/placebo/cheat variants pre-spend, fresh-context-reviewer hardened):
RED at HEAD → RED under a byte-noise placebo implementation (kills non-behavioral
discriminators) → GREEN under TWO independent fixes (SQL tie-break; Python-side
stable sort — kills source-text discriminators) → fixture whitelist + sha256
integrity. All three Null epochs produced genuinely verified regression tests
(full oracle sequence passed; verified from the raw .eval log, one pin
fingerprint `2f76c933…` continuous with the micro-run). Stock claude-sonnet-5
ran the test at HEAD, hit the placebo-pass, and iterated to a real RED without
the skill — the exact discriminating behavior the skill encodes. Screen cost
≈$0.57 (CW 57k / CR 1.30M / O 16.3k, token-computed).
INTERPRETATION (registered): subsumption evidence under the operator-accepted
transformative-only bar — sonnet-5-class agents no longer need this trap on its
own origin-derived home task; consistent with (and extending) the double-ceiling
finding, now 17/17 Null epochs across three independently-authored tasks. The
skill's disposition (retire / keep-as-historical) is the operator's call; the
datum for its EVIDENCE.md is p0=1 @ claude-sonnet-5, 2026-07-10. No BH-FDR entry
consumed (primary tests run only on paired verdicts; none launched).
**2026-07-10 — Skill 2/4, `bayesian-eval-discipline`: Stage-0 Null screen 3/3
PASS — CEILING (p0 = 1). NO paired run, per the pre-stated p0 < 1 gate.**
Task as registered (origin: this harness's own aggregation workload): implement
the PASS/FAIL aggregation stage — Beta(1,1) posterior tails at threshold
0.6/confidence 0.95 over 25 engineered clauses (30% ties, spread across more than one), with the
defensibility requirements stated as spec: derived sample-size reachability
bound reported as UNMEASURED(underpowered) with precedence over FAILED, and
family-wide FDR control at q=0.05 (BH over ALL clauses' pass-side tails, or a
more conservative procedure at the same level). Oracle (validated 11/11
pre-spend): 25 exact verdicts engineered to be INVARIANT across both honest tie
policies × BH/Bonferroni × include/exclude-underpowered family scopes, with
probe clauses that kill statistics-free win-rate heuristics (500×500-cutoff
scan: zero survivors), pin the floor to exactly n<5, spring the
precedence trap, and separate floor-less and correction-less implementations.
Fresh-context reviewer (independent recomputation) contributed the probe
clauses + two honest-implementation fairness fixes (full-family BH scope
wording; "stricter" pinned to procedure-not-level) — all applied and
re-verified before spend. All three Null epochs ORACLE-PASS (raw-log verified,
pin `2f76c933…`): stock claude-sonnet-5 implemented exact tails, derived the
floor, applied full-family BH, and emitted the reason-coded verdicts, unaided.
Screen cost ≈$0.75 (CW 79k / CR 397k / O 46.8k token-computed).
INTERPRETATION (registered): subsumption evidence under the transformative-only
bar — double-ceiling now **20/20 Null epochs across four independently-authored
tasks**. EVIDENCE.md datum: p0=1 @ claude-sonnet-5, 2026-07-10. No BH-FDR entry
consumed (no paired run). INSTRUMENT NOTE (registered honestly): under exact
tail computation the registered "minimum-N floor present" check is behaviorally
VACUOUS at verdict level (the floor is mathematically implied by the criteria);
it became testable only by adopting the skill's own reporting schema —
UNMEASURED(underpowered) vs UNMEASURED(inconclusive) — which forces the
reachability derivation. Screens of discipline-shaped skills must check the
REPORTING discipline, not just decision outcomes.

**2026-07-10 — Skill 3/4, `append-only-evidence-design`: Stage-0 Null screen
3/3 PASS — CEILING (p0 = 1). NO paired run, per the pre-stated p0 < 1 gate.**
Task as registered (origin: this repo's evidence-store domain): build an
evidence-store module whose verdict rows are immutable against the DATABASE
FILE ITSELF (any connection or tool, not just the authoring module), then
migrate it in place to add a range CHECK on an existing column — which SQLite
only permits via table recreation, and recreation silently drops triggers (all
three trap premises verified in the pinned image pre-authoring). Oracle
(validated 7/7 pre-spend): module-isolated architecture — the agent's module
executes ONLY in child processes; tamper probes and verifying reads run in
module-free child processes ("future scripts that never import this module",
literally); probes = raw UPDATE/DELETE pre- and post-migration, v1-unconstrained
probe on a file copy (kills the CHECK-in-v1 no-op-migration dodge), v2
out-of-range INSERT rejection, and append-still-works. Fresh-context reviewer
CONFIRMED the author-suspected in-process weakness with a complete working
exploit (module monkeypatches sqlite3.connect at import → fake immutability
with zero DB enforcement) — closed by the module-isolation rewrite, and the
exploit added to the validation matrix (now FAILS). Reviewer's optional
all-table tamper sweep was REJECTED on fairness grounds: it would fail honest
designs keeping legitimate mutable runtime state (the skill's own Discipline 2
endorses those). All three Null epochs ORACLE-PASS (raw-log verified, pin
`2f76c933…`): stock claude-sonnet-5 shipped BEFORE UPDATE/DELETE triggers AND
a trigger-recreating migration, unaided, 3/3. Screen cost ≈$1.16 (CW 103k /
CR 1.55M / O 59.2k token-computed).
INTERPRETATION (registered): subsumption evidence under the transformative-only
bar — double-ceiling now **23/23 Null epochs across five independently-authored
tasks**. EVIDENCE.md datum: p0=1 @ claude-sonnet-5, 2026-07-10. No BH-FDR entry
consumed. APPARATUS RULE (registered): oracles that import subject-authored
modules are in-process-compromisable BY CONSTRUCTION; the module-isolation
pattern (subject code only in child processes; probes module-free) is now the
standing shape for any import-the-module oracle.

**2026-07-10 — Skill 4/4, `llm-judge-calibration`: Stage-0 Null screen
INCOMPLETE — 2/2 scored epochs PASS; third epoch OWED (blocker: API credit
exhaustion, not apparatus).** Task as registered (origin: the Phase-B judge
workload): implement the judge admissibility gate around a caller-supplied
judge callable — prompt-hash-inclusive judge_id (one-character prompt edit
changes it; raw prompt never embedded; cross-process stability probe kills
hash()-based ids), position-swap double call (invocation count + swapped-args
recorded by oracle-held closures), pairwise-not-scalar schema (numeric verdict
→ invalid_verdict, never coerced), and the calibration floors with exact
boundary probes incl. rule-order precedence. Oracle validated 12/12 pre-spend.
Fresh-context reviewer found a HARNESS-LEVEL hole: the pass criterion was
exit-code-only, so a module doing sys.exit(0) at import would pass before any
assert ran — closed with a sentinel gate (pass = exit 0 AND "ORACLE-PASS" in
captured stdout), guarded import, and a PYTHONOPTIMIZE reset; retro-checked
the three already-run screens (no exploitation: sentinel present / module
isolation / no module import respectively). SENTINEL RULE now standing for
all import-the-module oracles alongside the module-isolation rule.
Both scored epochs genuine ORACLE-PASS (raw-log verified, pin `2f76c933…`).
Epoch 3 and two top-up attempts errored: claude-code exits 1 with EMPTY stderr,
which inspect_swe classifies as its retriable "scaffold bug" — root-caused via
direct API ping to **account credit exhaustion mid-screen** (CC 2.1.197
surfaces the billing error as a silent crash; apparatus note: empty-stderr
exit-1 from the agent is ambiguous between scaffold bugs and billing/API-deny
states — check account health before re-attributing). Adapter gained a
retry_uncaught_errors passthrough (resilience knob, excluded from the pin
fingerprint) — shipped regardless; it retries genuine scaffold flakes, not
billing failures. SCREEN VERDICT: PENDING the third epoch (pre-stated gate
needs 3; 2/2 so far is ceiling-consistent). The epoch will be run when
credits are restored; until then skill 4/4 carries no verdict — UNMEASURED
would be wrong (this is an apparatus gap, not a domain property).

APPARATUS INCIDENT (same day, voided run, ≈$1.11): the first screen launch was
voided pre-analysis — Inspect resolves `Sample.files` VALUES against the local
filesystem before falling back to inline content, and an empty-string fixture
file resolved to the runner's cwd, injecting the working directory (oracle + gold
assets) into the sandbox. The oracle's own whitelist check caught the injection
(3/3 scored fail on unexpected files — the guard worked against apparatus error,
not just subject cheating). Fixed in `c9ce497` (fix(subject): deliver sandbox
files as data URIs), regression-tested offline, and re-verified with a $0
mockllm in-sandbox layout probe before relaunch; the probe is now a standing
pre-flight for every fixture-bearing screen. Exposure confined to the voided
run's local logs; no persistent contamination (fresh sandbox + fresh context per
run; assets unpublished).

**2026-07-10 (later same day) — Skill 4/4 screen COMPLETED: third epoch run after
credit restoration → 3/3 PASS — CEILING (p0 = 1). NO paired run, per the
pre-stated p0 < 1 gate.** The owed epoch ran on the identical registered
apparatus (same runner, oracle, and pin — fingerprint `2f76c933…` verified in
the raw .eval log; status success, score C, sentinel "ORACLE-PASS: judge_id
digest-stable, swap protocol exact, floors exact" present). Account health
verified by direct API ping immediately before launch, per the apparatus note
above. Epoch cost ≈$0.97 token-computed (CW 342k / CR 133k / O 8.4k).
INTERPRETATION (registered): subsumption evidence under the operator-accepted
transformative-only bar — stock claude-sonnet-5 implemented the full judge
admissibility gate (prompt-hash judge_id, position-swap double call,
pairwise-not-scalar schema, exact calibration floors with rule-order
precedence) unaided, 3/3. **Batch-1 is now complete: all four screens
CEILING at p0 = 1; zero paired runs launched; zero BH-FDR primary tests
consumed (the q=0.10 family was declared and never drawn on). Double-ceiling
standing: 26/26 Null epochs across 6 independently-authored tasks.**
EVIDENCE.md datum for the skill: p0=1 @ claude-sonnet-5, 2026-07-10.
Disposition (retire / keep-as-historical) is the operator's call.

---

**2026-07-12 — PILOT-1 REGISTERED (commitment hash only; contents private).**
A pre-registration for a new screening pilot was fixed today and is held
privately pending a separate publication decision. To make the registration
verifiable after the fact without disclosing its targets, its SHA-256 is
committed here:

`81a89dc53d2a7f8c4601d905aee8e783c9341a5085b78e15457519158feffdec`

What can be said now: the pilot is Null screens only (no paired runs; the
standing p0 < 1 gate and the registered task-sourcing condition are unchanged);
the slate, selection rubric, task-derivation fairness clause, oracle protocol
(batch-1 standard, all standing rules), verdict vocabulary, compute cap, and
kill criteria are all fixed in the committed file. If and when results are
published, the file is disclosed verbatim and must hash to the value above.

---

## AMENDMENT 2 — OBS ledger pointer (2026-08-02, dated block per the amendment rule)

Non-substantive; alters no registered content. The historical Stage-0 screen
records registered above are re-scoped as per-record observation ledger
entries in [`docs/observations/`](../observations/README.md) (OBS-0001 through
OBS-0006; re-scoping semantics ratified in
[#41](https://github.com/MrBinnacle/skill-harness/issues/41)). The ledger is
canonical for per-record counts, scope pins, evidence basis, and
classification state; this document's registered text is unchanged and remains
the registration of record.

---

## AMENDMENT 3 — the treatment is exposure; the invocation detector is a stratifier (2026-09-01, dated block per the amendment rule; the ruling on skill-harness#384)

**What happened.** On 2026-09-01 a paired k=8 on `git-pull-rebase-trap` (one pin both
arms, `5324feef...`, de-leaked prompt) returned discordant x = 6 of 8 (Jeffreys 95%
[0.408, 0.944]), GO at the pre-stated x >= 5, Null 0 of 8. `write_paired_evidence`
refused the pair with `ZeroInvocationError`: zero Skill tool calls across the eight
Full-arm epochs. Finding: `pi-c-detector-blind-to-description-channel.md`. Receipt of record after
the 2026-09-02 re-ingest under detector v2:
`docs/sers/receipts/gitpull-paired-k8-2026-09-01-detector-v2.json` (admissible,
`CANT_TELL_YET`/`underpowered`). The refused 1.1.0 receipt stands unedited at
`docs/sers/receipts/superseded/gitpull-paired-k8-2026-09-01.json`.

**Measured 2026-09-01, on the two `.eval` logs through `read_eval_log`, first user
message of every epoch:** the Claude Code skill listing (`<system-reminder> The
following skills are available for use with the Skill tool: ...`) carries the card's
frontmatter description verbatim in 8 of 8 Full epochs and in 0 of 8 Null epochs. The
description channel is observable in the transcript. The finding's refuses-to-claim
line on that point is withdrawn by a dated correction on the finding.

**The registered construct is the treatment-policy strategy of ICH E9(R1) (#36,
adoption 1): the treatment is "the skill is available to the agent", and the model's
choice not to invoke the body is an intercurrent event that the strategy includes in
the effect.** The refusal predicate (#36, adoption 4) was written on identification
grounds: at pi_c = 0 the complier effect CACE = ITT / pi_c is undefined. That is true.
It does not bear on the treatment-policy contrast, which is the ITT contrast and is
identified at every pi_c, zero included. The refusal message adds a second argument,
"no effect cannot be distinguished from never invoked". That argument presumes the
exclusion restriction of the instrumental-variables reading: assignment moves the
outcome only through invocation (Angrist, Imbens and Rubin 1996, JASA 91(434),
"Identification of causal effects using instrumental variables"). The 2026-09-01 run
falsifies that presumption for this class of card: assignment moved the outcome 6/8
against 0/8 with invocation at 0/8. When the exclusion restriction fails, the
instrumental-variables quantity is not identified and the ITT contrast remains the
valid effect of assignment. The registered quantity of record is that contrast.

**Ruling.**

1. The treatment is EXPOSURE: the skill mounted for the arm, with its description
   present in the agent's context. Exposure is measured per epoch by a channel-(c)
   detector: the card's description text, read from the pinned `SKILL.md` frontmatter,
   present in the transcript's skill listing. `exposed_skill: bool` joins
   `invoked_skill: bool` on the parsed sample.
2. pi_c (invocation, detector v1 branch (a)) stays a MANDATORY recorded stratifier,
   with its Clopper-Pearson interval on every availability verdict (#36, adoption 4,
   unchanged). It no longer gates the write.
3. Refusal predicates at paired ingest after this ruling: (a) a Full-arm epoch with
   exposure not detected refuses the pair as an apparatus error (the treatment was not
   delivered); (b) a Null-arm epoch with exposure or invocation detected refuses the
   pair as control-arm contamination (the #46 rule, widened to channel c); (c) zero
   invocations with full exposure is ADMISSIBLE: the write proceeds, records
   pi_c = 0/n with its interval, and the verdict line carries it.
4. A verdict at pi_c = 0 reads: the value arrived through the description (the
   standing line) and the body was not read. It is a scoped treatment-policy verdict
   under the model-pull delivery class (#36, adoption 2), and the pi_c line is what
   tells a reader so. No new verdict state is introduced. An EQUIVALENT at pi_c = 0 is
   a no-lift finding for the mounted card under model-pull; it says nothing about the
   body under a hook-nudged registration, which is a separate registration by
   adoption 2.
5. The optional CACE secondary (#36, adoption 4) is reported only when exposure and
   invocation both vary. At pi_c = 0 the receipt states it as not identified, with
   the exclusion-restriction caveat, and never computes it.
6. SERS already splits the cost of a skill's two products, `standing_tokens` for the
   always-loaded description and `fired_tokens` for the on-invocation body. Value
   attribution now matches that split: a receipt carries the exposure rate, pi_c with
   its interval, and the channel reading. The schema change is additive (a later SERS
   minor version) and is ticketed, not made here.

**The 2026-09-01 pair under this ruling.** Admissible once detector v2 lands and the
pair is re-ingested. It remains a Stage-1 micro-run under this registration: k = 8,
the GO datum for the sized run, no win-direction field, no verdict claim
(Tracer-bullet scope; Stage-1 analysis rules). Its receipt is superseded, moved and
not edited, by one that records the pair as admissible and the verdict as
CANT_TELL_YET with sub-reason `underpowered` against the design in Amendment 4. A KEEP
of record needs the sized confirmatory run.

**Route.** The registration names the direct Anthropic API. The pilot ran on
OpenRouter, declared before launch; the host holds no Anthropic key at any scope
(measured 2026-09-01, Process, User and Machine). The sized run runs on the registered
route. Admitting OpenRouter for a sized run is a route amendment that needs its own
dated block with a both-arms-same-route clause, and it is not made here.

*Revisit if:* a non-`claude_code` solver lands whose transcript does not carry the
skill listing (channel c then needs a solver-seam record, the candidate named in the
finding); or a Full-arm epoch is found with the listing present in the transcript while
the model's input demonstrably lacked it (the transcript would then not be the model
input, and the detector would be reading the wrong surface); or a second card shows
exposure 8/8, invocation 0/8 and NO effect, which is the first live test of ruling
point 4.

---

## AMENDMENT 4 — Gate-2 design for the paired lane: registered frontier and row candidates (2026-09-01, dated block; skill-harness#368 items 2 and 3)

**Why here.** INVARIANTS section 8 rules the discordant table the registered quantity
of record and records that the locked 0.60 / 0.95 / 0.05 thresholds do not transfer to
it: re-derivation must be pre-registered before a production run consumes the result.
This block is that pre-registration for the paired (subject) lane. It registers the
design form, the alternative region, the evaluation grid, the cost basis, the frontier
and the conforming rows. The numeric row-pick is a human act (#37 item 7, D-3:
thresholds are earned, never authored) and is recorded by the operator's signature on a
ratification record after this block and before spend.

**Design form (#37 item 3, unchanged):** `Gate2Design(n_pairs, gamma, MMESpec(delta_min,
q_min))`; three-sided rule over the discordant lattice; deterministic curtailment on;
decision to verdict through `effect_from_matched_gate2` and `matched_gate2_verdict`,
with `value_class` supplied by the caller.

**Registered alternative region H1 and evaluation grid.** The detectability disclosure
above fixes the bar: transformative only, Null pass <= ~0.3 and Full pass >= ~0.8, so
delta = d(2q - 1) near 0.5 with q near 1. The analysis rules size at the ~25th
percentile of the pilot's d posterior, read as an UPPER bound on population d, for a
task mixture. Pilot d posterior, Jeffreys Beta(6.5, 2.5): 25th percentile 0.631, 95%
interval [0.408, 0.944]. Registered alternative grid, every point inside H1 at
delta_min = 0.20 and q_min = 0.70:
(0.5, 1.0), (0.6, 0.9), (0.6, 1.0), (0.75, 0.9), (0.75, 1.0), (0.9, 0.9).
The binding point at every row is (0.6, 0.9). Null nuisance grid d in {0.1, 0.2, ...,
1.0}, with d = 1 added by the engine. Power floor 0.80. Candidate false-direction
target 0.05 (#37 item 5), tested on the certified Bernstein upper bound and never on
the grid maximum.

**Cost basis (#40(c), live projection, no cache discount).** Measured 2026-09-01 pilot
tokens per pair across both arms: 353,721 input (all classes) and 2,230 output.
`project_pair_usd("claude-sonnet-5", ...)` = $0.7297 per pair, the registration's own
subject model. At `claude-sonnet-4-6` pricing the same volume is $1.0946 per pair and
every row n >= 32 exceeds the $35 cap. The pilot's realised spend was $0.26 per pair
with 86% cache reads; the cap tests the worst case by rule.

**Frontier, computed 2026-09-01 with `oc.frontier.frontier_row` (exact enumeration,
curtailed operating characteristics; script and full table held with the session
record):**

| gamma | delta_min | first conforming n | at that n: alpha[cert] / power(H1 min) / worst-case $ | at n = 32 | at n = 40 |
|---|---|---|---|---|---|
| 0.95 | 0.20 | 40 | 0.0034 / 0.808 / $29.19 | 0.0052 / 0.691 / $23.35 | 0.0034 / 0.808 / $29.19 |
| 0.90 | 0.20 | 31 | 0.0161 / 0.803 / $22.62 | 0.0161 / 0.826 / $23.35 | 0.0095 / 0.892 / $29.19 |
| 0.85 | 0.20 | 26 | 0.0433 / 0.826 / $18.97 | 0.0294 / 0.885 / $23.35 | 0.0201 / 0.931 / $29.19 |
| 0.80 | 0.20 | 31 | 0.0428 / 0.913 / $22.62 | 0.0501 / 0.920: fails alpha | 0.0385 / 0.958 / $29.19 |
| 0.90 | 0.25 | none | best row n = 40: 0.0034 / 0.760 / $29.19 | | |
| 0.95 | 0.25 | none | best row n = 40: 0.0011 / 0.624 / $29.19 | | |
| 0.95 | 0.30 | none | best row n = 40: 0.0003 / 0.405 / $29.19 | | |

Two facts the table establishes. (1) gamma is not the error rate (#37 P3 said so from
the other side): at delta_min = 0.20 and gamma = 0.95 the attained worst-case
false-direction rate is 0.003, fifteen times under the target, and the price is power,
0.81 at the top of the grid only. (2) At delta_min >= 0.25 no row on the ratified grid
reaches 0.80 power inside the cap at sonnet-5 pricing: the discordant-lattice rule
cannot promise 80% power to a margin of 0.25 at the transformative alternatives with 40
pairs. Stated here so the row-pick does not go looking for it.

**Conforming rows (alpha[cert] <= 0.05, power >= 0.80, worst-case <= $35):**
gamma 0.85, n in 26..40; gamma 0.90, n in 31..40; gamma 0.95, n = 40; gamma 0.80, n in
{31, 34, 35, 36, 37, 38, 39, 40}.

**Recommended row: gamma = 0.90, delta_min = 0.20, q_min = 0.70, n = 32** (alpha[cert]
0.016, power 0.826 at (0.6, 0.9), worst-case $23.35, E[N] 29.9 under H1). Reason: a
three-fold margin under the 0.05 target with eight fewer pairs than gamma = 0.95 needs,
and $5.84 under that row. The gamma = 0.85, n = 26 row is the cheapest conforming
($18.97) and sits at 0.043 on the certified bound, one grid step from the target; a
reader who weighs cost over margin picks it. The pick is the operator's by #37 D-3 and
#40 Q3. What changes the recommendation: a pre-spend re-measurement of tokens per pair
above about 470k (n = 32 then breaches the cap at sonnet-5 pricing), or a pin to a
model outside the pricing table.

**What the registered design does to the pilot.** k = 8 is not n_pairs.
`aggregate_matched_gate2` refuses a count mismatch (`COUNT_MISMATCH`), and the
paired-lane bridge inherits that refusal: the pilot cannot receive a Gate-2 decision
under this design, by construction, and is not read against it. The pilot's role is the
GO datum and the d posterior above.

**Binding.** This block registers the form, the region, the grid, the cost basis and
the conforming set. The row (n, gamma) is registered when
`docs/ratifications/RAT-0001-git-pull-rebase-trap.md` flips to RATIFIED with the
operator's signature (README signing order). SME branch: the #45 clock never started
(#59 record), so the record carries the self-certified disclosure line "internally
derived, not externally deliberated" with the expiry arithmetic stated. No spend before
that. The paired-lane runner sits outside the `run ablation --execute` gate; the
ratification reference goes into the runner's config and the ingest records it in
`config_json` (ticketed with the Path C bridge).

*Revisit if:* the pricing table changes (recompute the cost column; the table is
provenance, not the pick); the ratified grid ceiling moves (#40's revisit clause); or a
pre-spend re-measurement of tokens per pair moves the worst case past the cap.
