# Pre-Track-D Council Fire — Raw Seat Outputs (verbatim)

Dispatched 2026-06-06, Opus 4.7, parallel, read-only. 5 seats: STAT, COST, RELIABILITY, OPERATOR-DX, EVAL-RESEARCH. Synthesis: `synthesis.md` (this directory).

---

## SEAT: STAT (agentId aadf0dd1b71779cfb · 112,655 tok · 15 tool uses)

Verification preface: 2409.09951 CONFIRMED (operator matters; "3-9x swing" magnitude NOT abstract-confirmed — directional claim cited, magnitude flagged landscape-sourced). 2405.20404 = "JoPA: Joint Prompt Attribution" (Chang et al.) CONFIRMED on combinatorial/non-additivity; landscape's "XPrompt/JoPA" label corrected to JoPA. 2409.00729 ContextCite CONFIRMED title/authors (random-subset surrogate is the method; abstract didn't expose internals). 2312.15395 "Prompt Valuation Based on Shapley Values" CONFIRMED but it is whole-prompt-ensemble valuation, NOT within-prompt component decomposition — material correction to landscape framing.

STAT-1 / N_max-without-stop = UNMEASURED(underpowered); interim-test alpha inflation bounded by Bayesian-posterior semantics not frequentist alpha-spending. Severity MAJOR. Anchor A8 / A17 `underpowered`. Bayesian posterior is a valid evidence summary at every n (always-valid under optional stopping for the posterior itself); what inflates is long-run cross-clause frequency = multiplicity (STAT-7), not a single-clause Type-I defect. Lan-DeMets alpha-spending NOT needed (stop is Bayesian-posterior, not a t-test). Rec: hard stop emitting UNMEASURED(underpowered) with achieved posterior recorded; record stopping_reason ∈ {passed, failed, underpowered_nmax, budget_exhausted}; NO batches past N_max. Disposition TRACK-D-EXIT-CRITERION.

STAT-2 / Confound threshold k=2.0, σ(Null) per-(run,axis) with N_null≥30 floor. Severity MAJOR. Anchor A11 / confound_events.py:10-12. A11 locks k=2.0 and N_null≥30; open question is estimation scope → per-(run,axis) at write-time (recomputing from pooled cross-run corpus at read time would violate write-time-snapshot). Runner scores EVERY sample on ALL registered axes (4 Tier-1 metrics cheap vs API cost). If N_null<30 for an axis, confound detection disabled for the run, affected verdicts UNMEASURED(underpowered). Uncalibrated Tier-2 axes excluded from σ(Null). Disposition ADOPT-AS-A39+.

STAT-3 / FLAGGED_CONFOUNDED is a read-time JOIN, NOT a verdict-row state — correct per existing schema. Severity MAJOR. Anchor A29 / oracle_verdicts.py:18 (CHECK admissible|inadmissible) / migration 0003 VIEW. Confounding recorded as independent append-only confound_events row; exclusion via VIEW anti-JOIN. Two orthogonal append-only facts joined at read — NOT recomputing admissibility. Rec: write confound_events at detection time, not a verdict mutation. CRITICAL: confirm VIEW JOIN uses primary_clause_id = ABLATED clause; RED test before orchestration code (insert confound_flagged on N, assert N's verdict drops from VIEW, M's does not). Disposition TRACK-D-EXIT-CRITERION.

STAT-4 / Raw clause DELETION is a BLOCKER to clean deltas; mandate versioned matched-length neutral substitution. Severity BLOCKER. Anchor landscape §5a / arXiv:2409.09951 VERIFIED. Deletion conflates clause axis-effect with coherence/length/format loss; verbosity is a registered Tier-1 axis so length-loss from deletion directly contaminates one of our own four axes — self-evidently disqualifying. Rec: AblationOperator = versioned explicit artifact; v0.1 default = matched-length semantically-null placeholder; store ablation_operator_id + version on run config snapshot; deterministic layer constructs substituted prompt (control-flow ownership). Disposition ADOPT-AS-A39+.

STAT-5 / Redundancy cancellation = documented v0.1 limitation; paired-ablation probe gated, ONLY for UNMEASURED clauses. Severity MAJOR. Anchor landscape §5a / arXiv:2405.20404 (JoPA) VERIFIED. FLAGGED_CONFOUNDED cannot catch within-axis cancellation (no contamination signal). v0.1 = documented limitation; narrow probe behind --probe-redundancy (off by default, budget-charged); probe only reclassifies UNMEASURED(no_signal)→UNMEASURED(redundant_load_bearing), never →PASSED. Full random-subset surrogate = v0.2. Disposition DOCUMENTED-LIMITATION (probe = optional flag).

STAT-6 / Report Contribution as lower-bound-under-redundancy (n=1 Shapley special case); random-subset surrogate v0.2. Severity MAJOR. Anchor landscape §5a / Q11 / 2409.00729 / 2312.15395 (framing CORRECTED — 2312.15395 is whole-prompt-ensemble, not within-prompt component). LOO delta is the all-others-present marginal = biased-low under redundancy. Track E field label `Contribution (single-clause LOO; lower-bound under redundancy)`. Track D stores condition-labeled samples (already does) so v0.2 surrogate consumes without re-sampling. Disposition DOCUMENTED-LIMITATION + DEFER-V0.2 (surrogate).

STAT-7 / Multiplicity control lives in Track E (hierarchical Beta-Binomial per A9), but Track D MUST persist per-comparison metadata. Severity MAJOR. Anchor A9 / landscape (BH-FDR). K=10×4 axes = 40 comparisons, ~2 expected false passes at nominal 5%. A9 hierarchical hyperprior self-immunizes; BH-FDR fallback. Track D obligation: every verdict tagged (run_id, clause_id, axis, comparison) + run config records family size K×|axes|. Disposition TRACK-D-EXIT-CRITERION (metadata); A9 owns correction.

STAT-8 / Operator-self-label produces structurally invalid axis-level κ — HOLD REFUSE for v0.1. Severity MAJOR. Anchor C2 / routing note 2026-06-05. Defect is NOT κ≈1.0 (distinct raters) — it is that κ certifies "agrees with THIS operator's preferences," never "measures axis X." Calibration's purpose is an axis-level admissibility claim; self-labeling collapses it to operator-preference. Flip's own guardrail (pool must reject self-labeled) concedes non-evidence-grade. COST's "ships testable" met by synthetic JSONL fixture. Rec: no operator_self_labeled admissible tier; if DX affordance wanted, clearly-NON-admissible state, hard-excluded at repo layer, [NOT EVIDENCE-GRADE] flag, cannot flip to PASSED — v0.2 (D16), not v0.1. NOT surfaced to user — decided here. Disposition VALUE-DECISION resolved by SME → HOLD REFUSE; bootstrap = DEFER-V0.2.

Cross-talk: COST right that all-axes scoring + substitution have token implications (substitution sits at A13 cache boundary — verify placeholder tokens don't bust breakpoint); COST wrong if it argues self-label worth admitting for cost/DX; COST may under-weight paired-redundancy probe budget. RELIABILITY right on crash-recovery of write-confound-events-at-detection-time (partial append-only rows authoritative-but-incomplete); owns N_max×mid-batch-crash interaction; may under-weight that resumed re-scoring corrupts sequential n. OPERATOR-DX right that UNMEASURED(underpowered)/lower-bound labels need operator-legible surfacing; wrong to push single-number summary (violates no-scalar-collapse). EVAL-RESEARCH right on prior-art precision (will catch that "n=1 Shapley special case" is loose — LOO is grand-coalition marginal, biased-low, not degenerate-unbiased-Shapley); may under-weight write-time-snapshot constraints on v0.2 surrogate retrofit. status: nominal.

---

## SEAT: COST (agentId a6b512e5dc1cb1bbc · 109,530 tok · 10 tool uses)

Preface: Track C documents cache discipline but the judge call passes system=system_prompt as a plain string with NO cache_control breakpoint — Track C's "~71% cache reuse" is aspirational, not realized. Load-bearing for COST-1.

COST-1 / Cache breakpoint placement (A13). MAJOR. Anchor judge.py:413 + A13. Realized judge call has zero cache_control markers (grep confirms). SDK: cache_control:{type:ephemeral} is a property on the LAST content block of a cacheable span — system must be a list of typed blocks, not a string. A13 "end-of-system AND end-of-skill-prefix" = TWO breakpoints (max 4). Cacheable = system+skill_prefix (clauses before ablated one); unique = ablated-clause-last tail + subject task. Ablated-clause-LAST claim is CORRECT (maximizes shared prefix across the K conditions of ONE clause). Prefix shared WITHIN a clause's K conditions, NOT across different clauses M — cache reuse is K-local not M-global. Rec: system=[{type:text,text:...,cache_control:{type:ephemeral}}]; second breakpoint on last skill-prefix block; render Null-shared-prefix→per-clause-suffix→ablated-clause-last→subject-task (uncached). Cache-marker assertion test (mock at SDK boundary per A32). Disposition ADOPT-AS-A39 + TRACK-D-EXIT-CRITERION.

COST-2 / Budget check inside writer transaction (A12/A26). MAJOR. Anchor runtime/0001:48-59 (run_budget), A26, A12(b). run_budget comment already says "reads+updates inside same transaction as sample insert." Race real only if cap check reads usd_spent outside writer txn. Under A26 (BEGIN IMMEDIATE + busy_timeout, single-threaded v0.1) the read-modify-write serializes; two checks cannot both pass before either writes. Race reappears only at D11 multi-process. Subtlety: pre-call check uses PROJECTED next-call cost (worst-case gate); post-call ledger reconciles to actual. Rec: cap check + ledger read inside same writer_transaction(runtime) as the budget UPDATE; test_budget_check_serializes mirroring test_concurrent_writers_serialize.py. Disposition ADOPT-AS-A40.

COST-3 / Cost ledger atomic write + volume (A12). MINOR. Anchor runtime/0001:62-77. Volume bounded: 10-clause skill ≈ 1,200 subject rows + judge rows — thousands not millions. idx_cost_ledger_ts + idx_cost_ledger_run already present. Per-call append correct (batching defeats crash-resumability). Reconciler gap: ledger is estimate; response usage block is ground truth; drift if ledger writes projected not response-reported tokens. Rec: write ledger from actual response usage (input/cache_creation/cache_read/output tokens); run_budget.usd_spent increments from same actual usage; skill audit reconciler (D7-adjacent) sums per run_id vs run_budget.usd_spent (mismatch = dual_write_partial signal). Disposition ADOPT-AS-A41 + reconciler DEFER-V0.2.

COST-4 / cache-warmup (A36 _warmup_first_call). MAJOR. Anchor calibration/command.py:198,397. Track D needs warmup; cost of getting wrong larger here. Concurrent fan-out before first cache-write completes = every parallel call a cache MISS (10× price). v0.1 single-threaded (A26) naturally serializes within a clause, but cache-write must LAND before second call reads; back-to-back synchronous calls satisfy this. Real failure mode = batching/parallelizing (D11) without re-establishing warmup. Rec: reuse _warmup_first_call per (clause, condition-group); projection cache_reuse_pct reflects prefix-amortization-over-K×N; document v0.1 single-thread satisfies warmup implicitly, D11 must re-add. Disposition ADOPT-AS-A42.

COST-5 / neutral-substitution vs deletion cost angle (STAT-4). MINOR/OBSERVATION. Anchor landscape §5a / 2409.09951 / A13. Cost interaction FAVORABLE not adverse: matched-length placeholder keeps skill-prefix token count constant across Full vs Ablated_k, stabilizes cacheable prefix, makes projection deterministic (deletion shrinks prefix, perturbs counts). Tiny fixed placeholder cost; cleaner length-matched prefix. Caveat: placeholder must be byte-stable per (clause,version) or busts cache each run. Rec: if STAT-4 adopts substitution, COST concurs; require deterministic placeholder (frozen per clause, clauses.rendering_index provenance). Do NOT let cost justify keeping deletion. Disposition ADOPT-AS-A43.

COST-6 / C2 COUNTER. OBSERVATION. Anchor §C2 / llm-judge-calibration 3-6. PRO: operator-self-label lowers cost-to-first-result, ships v0.1 testable without external rater infra (dominant v0.1 cost per landscape §3). BUT COST concedes, HOLDS REFUSE: a saving that produces non-evidence-grade verdicts the pool must reject is not a saving; landscape §4 names the calibration corpus as the moat, a self-labeled corpus is worthless as the asset (its "agrees-with-operator" basis is exactly what third-party-audit buyers distrust). Rec: HOLD REFUSE; if ever flipped (D16 opt-in) it's demo/smoke only, never evidence, cost-accounted separately. Disposition VALUE-DECISION resolved by STAT — COST concurs REFUSE.

Cross-talk: STAT right neutral-substitution is highest-leverage and cost-neutral (don't trade away on budget); on C2 my PRO collapses under independence objection; WATCH redundancy probe doubles call count for UNMEASURED clauses — budget needs +joint_probe term. RELIABILITY right COST-2 race-safe only because A26 single-threaded; D11 breaks both together; cost_ledger is ALSO the crash-recovery ledger — resumed run must SUM existing rows or double-spends. OPERATOR-DX right dry-run must print COST-1 projection same formula as execute; WRONG that calibrate dry-run reusable verbatim (uses MagicMock conns, TC-SLOP-001); under-weight that progress must surface running $ vs cap. status: nominal.

---

## SEAT: RELIABILITY (agentId a3521c8b2c33a1623 · 90,647 tok · 10 tool uses)

Preface facts: samples has no (clause_id,condition,sample_index) natural key — only sample_id PK, no idempotency key. run_progress has samples_planned/collected/last_heartbeat but no record of WHICH samples. oracle_verdicts stores only claimed axis; confounds in separate confound_events. cost_ledger write in SEPARATE runtime txn from evidence verdict (dual_write.py:87-105) — under-count window real/structural. A20 trigger: completed_at settable once. terminal_state enum (aborted_budget/crashed_reconciled) DEFERRED to v0.2 (COUNCIL_FINDINGS:332) — no terminal-state column to write run end-reason.

REL-1 / runs.completed_at single-shot transactional shape. MAJOR. Anchor evidence/0001:203-205 (A20 trigger), dual_write.py:108-142. A20 enforces immutability-after-set, NOT when/that-reached. Crash at 1,732 leaves completed_at=NULL permanently; no terminal_state (deferred). No completion dual-write helper exists (only write_run_start_with_budget). Rec: write completed_at via dedicated single-statement writer_transaction(evidence) UPDATE issued once after last verdict commits, gated by run_progress.samples_collected==samples_planned; never inside per-sample loop. Crash-vs-complete discriminator = (completed_at IS NULL AND run_progress.state != 'completed'); set run_progress.state terminal value as LAST runtime write. Disposition ADOPT-AS-A39.

REL-2 / idempotency key + resumability. BLOCKER. Anchor evidence/0001:96-106. samples has NO idempotency key (only content-derived sample_id PK). On resume cannot ask "did I write sample 1,732?". Replaying re-issues paid calls + writes duplicates that inflate Beta-Binomial w/n. Append-only makes double-counting UNCORRECTABLE. run_progress.samples_collected is scalar (counts how-many not which). Rec: migration 0300 UNIQUE(run_id,clause_id,condition,sample_index) + sample_index INTEGER NOT NULL. Failure policy: retry-bounded-backoff (429/500/network) / skip-and-record (400) / abort-run (budget). Resume = read existing tuples, set-difference vs frozen plan in runs.config_json, call only missing slots. UNIQUE makes double-write fail loudly not silently inflate n. Kill-at-1,732 test resumes to exactly 4,000 never 4,001. Disposition ADOPT-AS-A39 + TRACK-D-EXIT-CRITERION.

REL-3 / storage-volume (co-owned STAT). MAJOR. Anchor 0001:142-154 (confound_events), A11. Naive per-(sample×axis) = 16,000+ rows/run of mostly non-events. Schema already prevents: confound_events written ONLY when delta>k·σ_Null. Durable-but-bounded = threshold-triggered event rows, not dense matrix. Rec: compute all-axes deltas in-memory per condition-cell, persist confound_events row only on threshold cross; raw per-sample axis vector transient. Document so future implementer doesn't add dense axis-value table. O(confounds) not O(samples×axes). Disposition ADOPT-AS-A39.

REL-4 / budget-cap race (co-owned COST). MINOR. Anchor transaction.py:39 (BEGIN IMMEDIATE), A26, A12. Under A26 two budget-mutating txns cannot commit concurrently (second blocks). Failure only if check-and-decrement in separate txns. Rec: budget read + reservation write inside ONE writer_transaction(runtime); single-process v0.1 sufficient, no locking beyond BEGIN IMMEDIATE. Disposition TRACK-D-EXIT-CRITERION.

REL-5 / confound-flag write-time durability (co-owned STAT). MAJOR. Anchor CLAUDE.md confound handling / A29 / write-time-snapshot. Confound determination needs all-axes deltas across full condition-cell = cell-level computation not per-verdict; verdict write (per-pair) happens before cell complete. Resolved by two-table design — do NOT denormalize. Sequence: collect cell samples → compute all-axes deltas in memory → write oracle_verdicts (admissibility stamped write-time, available) → write confound_events for threshold-crossing axes same step. Confound EXCLUSION computed at READ time by VIEW (A29), not stamped on verdict — does NOT violate write-time-snapshot (invariant binds admissibility/calibration, not confound geometry). No verdict read-back forced. Disposition ADOPT-AS-A39.

REL-6 / ledger reconciler durability (co-owned COST). BLOCKER. Anchor dual_write.py:87-105. cost_ledger write in separate runtime txn committed AFTER evidence verdict; helper SWALLOWS runtime failure (no re-raise, lines 94-105). Die between evidence COMMIT and runtime COMMIT → spend happened, ledger under-counts → daily-cap sum reads low → budget over-run possible. Asymmetric durability working against cost safety (runtime NORMAL + re-derivable, but here it's the ONLY spend record; ablation verdicts have no cost column). Rec: (1) make cost re-derivable from evidence — oracle_verdicts/samples carry input_tok/cache_read_tok/output_tok/usd written INSIDE evidence transaction (durable FULL); cost_ledger becomes projection; post-crash reconciliation replays evidence cost columns. (2) reconciler at run-start + skill audit (D7) compares Σevidence.usd vs Σcost_ledger.usd per run, back-fills. Append-only-safe (new columns, migration 0300). Kill-between-commits test shows reconciler restores true spend. Disposition ADOPT-AS-A39 + TRACK-D-EXIT-CRITERION.

Cross-talk: STAT right redundancy-cancellation invisible to durability lens (redundant clauses crash-resume cleanly yet jointly mis-measured); STAT may treat resumed-partial-N as equivalent to fresh-N but resumed run carries duplicates without my UNIQUE constraint — STAT's n trustworthy only if idempotency key lands first; C2 durability-neutral. COST right cache-reuse ordering real; COST cannot treat cost_ledger as authoritative (REL-6 under-count) — cap must read re-derivable evidence cost columns; co-own REL-4/REL-6; reservation-in-one-transaction is the mechanism COST's cap depends on. OPERATOR-DX right "insufficient evidence" at N_max is also a reliability state needing recorded run_progress.state+reason (same write as REL-1 discriminator); resumability invisible unless run ablation prints "resuming from 1,732/4,000". EVAL-RESEARCH (not seated): A29 JOIN directionality (primary vs affected) load-bearing for REL-5 — wrong direction leaks confounded verdicts despite durable confound_events. status: nominal.

---

## SEAT: OPERATOR-DX (agentId ad40ccdf3cd0baf91 · 85,401 tok · 10 tool uses)

ODX-1 / Dry-run output shape + --execute gate. MAJOR. Anchor cli/main.py:127-134 (stub) vs cost_projection.py:75-214 / A12-(a). Mirror shipped calibrate dry-run (_print_calibrate_result:306-323), not a new shape — operators learn one projection grammar. Ablation structure calibrate lacks: per-clause×3-conditions, clauses vacuous-excluded (PRD §13→A17). Print: total N calls, input split (cache-read/uncached/unique-tail per A13), T_out, ≈$X on model, cache reuse %; AND per-clause table (N per condition-pair, est_CI_95_width/N-projection, VACUOUS-EXCLUDED marker). Single rolled-up number hides which clauses skipped. cost_projection.py:113-114 already emits est_se + est_ci_95_width; A17 falsifying_case_missing = UNMEASURED-by-construction must show as excluded-pre-run. Rec: (1) A12-(a) one-liner identical to :306-323; (2) per-clause Rich table clause#|axis|conditions|N_proj(min..max)|est_CI_width|status{TESTABLE|VACUOUS-EXCLUDED|NO-FALSIFYING-CASE}; (3) footer total. --execute is the gate; confirmation only above spend floor (projected usd>--max-usd refuses, mirror calibrate:284-286); no interactive prompt (breaks non-interactive dispatch). Disposition ADOPT-AS-A39 + TRACK-D-EXIT-CRITERION.

ODX-2 / Progress reporting. MAJOR. Anchor PLAN D:195-211 / A12-(b). N_max=40×3×K = thousands of calls. Zero progress = can't tell live from hung, can't see cost approaching cap. CLI has rich.console but no rich.progress. Required during --execute: per-clause/per-condition progress bar; running cost vs --max-usd AND --daily-cap; ETA; live stop-reason indicator (which clauses hit PASS/FAIL stop vs still sampling). Stop is per-condition-pair → progress per-clause not global. Rec: rich.progress one task per clause; footer "spent $X / cap $Y (run) · $Z / $W (day)"; emit from deterministic orchestrator NOT model workers (control-flow ownership); on --max-usd abort print partial-results summary not bare traceback. Disposition TRACK-D-EXIT-CRITERION.

ODX-3 / resume UX (cross RELIABILITY). MAJOR. Anchor checkpoint Q5 / A29 VIEW. Append-only makes committed verdicts durable → resume is not replay, it's "compute what's admissible + sample the deficit". PRD §18 has NO resume command, LOCKED says no-new-commands → resume within run ablation (re-run --execute against existing run_id is idempotent-additive). completed_at IS NULL = resumable; complete run not. Rec: run ablation <skill> --execute --resume <run_id> (flag, allowed) prints resume preview "Resuming run X: 1,732/4,000 done, M complete, K partial; spent $X; projected finish $Y". Bare run ablation with incomplete prior run WARNS + names resumable run_id (no silent fresh-start/double-spend). Coordinate cell-completion query with RELIABILITY. Disposition ADOPT-AS-A40 + TRACK-D-EXIT-CRITERION.

ODX-4 / UNMEASURED must not read as FAILED (cross STAT). BLOCKER. Anchor PRD §15:558-576 + §19.2:692 ("Distinguish failed from unmeasured") / A17:124-126. Stated success criterion → failing it is BLOCKER. N_max without PASS/FAIL = UNMEASURED(underpowered) not FAILED; operator misreading "fixes" an unmeasured clause. Need visually+lexically distinct surface + distinct exit code. Sub-reasons: no_data|inadmissible|underpowered|falsifying_case_missing|budget_exhausted. Rec: FAILED red "FAILED (P(win)≤.05)"; UNMEASURED yellow "UNMEASURED(<subreason>)". Exit codes 0=all reached verdict (PASS/FAIL), 2=≥1 UNMEASURED (CI branches without parsing stdout), non-2 reserved for hard errors. Never collapse UNMEASURED into generic failure. Disposition ADOPT-AS-A41 + TRACK-D-EXIT-CRITERION.

ODX-5 / ablation-operator transparency (cross STAT+EVAL-RESEARCH). BLOCKER. Anchor landscape §5a:102-107 vs A13:93-95 / CLAUDE.md Metric provenance. Operator-substitution swings importance 3-9×, repo has no policy (stub does nothing, :131). LATENT COLLISION: A13 reorders ablated clause LAST = deletion-style ("system+skill_minus_last_clause"); neutral substitution = prefix+placeholder breaks A13 shared-prefix cache assumption. Whichever wins, operator can't trust a verdict without SEEING exact rendered Ablated_k + operator-version. Provenance-of-ablation-operator = same class as metric-version provenance. Rec: (1) STAT/EVAL-RESEARCH decide deletion-vs-substitution; (2) run ablation --show-rendered <clause_id> prints verbatim Full/Ablated_k/Null side-by-side + ablation_operator_version; (3) record ablation_operator_version on every verdict write-time; (4) council resolve A13-cache vs substitution tension NOW not mid-build. Disposition ADOPT-AS-A39+ + flag A13-collision to STAT/COST.

ODX-6 / naming + error-path landmines + TC-SLOP pull-forward. MAJOR. Anchor cli/main.py + checkpoint TC-SLOP-001/002/012:129. (a) Silent dry-run gravest — operator forgets --execute, sees projection thinks run happened. Make banner impossible to miss + final line "NO CALLS MADE — re-run with --execute". (b) --max-usd vs --daily-cap ambiguous (calibrate:178-194); distinct errors: aborted_budget(run) cites --max-usd; daily-cap refusal cites --daily-cap + trailing-24h sum. (c) TC-SLOP-001: calibrate dry-run passes MagicMock() conn (:263,270-271) — Track D must branch before conn constructed, not inject mock. (d) TC-SLOP-002: JudgeClient() eager (:246) requires API key on dry-run — Track D dry-run must not require credentials to project cost. Rec: exit gate includes (i) terminal line; (ii) two distinct cap errors naming offending flag; (iii) dry-run constructs no conn/no JudgeClient (pure-offline, mirror project_calibration_cost:122-128 token-only); (iv) test dry-run with no ANTHROPIC_API_KEY. Disposition ADOPT-AS-A39+ (i,ii,iii) TRACK-D-EXIT-CRITERION; (iv) recommend in-track.

Cross-talk: STAT right redundancy-cancellation invisible at CLI (two load-bearing clauses both render UNMEASURED(underpowered)) — if paired-probe adopted I need a 5th sub-reason or distinct status so exit-code contract doesn't lump redundant-load-bearing with truly-absent; may treat N_max as purely statistical but it's operator's most confusing moment — name sub-reason CLI can render verbatim. COST: A13 cache (ablated-last) and my neutral-substitution transparency COLLIDE — substitution puts placeholder in prompt breaking shared-prefix cache; cost both operators; dry-run projection must be offline (no JudgeClient/key — give offline tokenizer path, Track C pinned tiktoken). RELIABILITY: owns which verdicts survived, I own resume command surface; need exact "resumable" predicate; WRONG if resume proposed as new command (violates §18 lock) — must be flag. EVAL-RESEARCH (side): A29 confound JOIN directionality surfaces at CLI — rendering CONFOUNDED clause, operator needs which other clause contaminated it → JOIN direction is operator-facing not just storage. status: nominal.

---

## SEAT: EVAL-RESEARCH (agentId a9de7739fb55281ca · 88,873 tok · 12 tool uses)

Verification (5 spot-checks at primary source): 2409.09951 (Li & Janson, Optimal ablation) VERIFIED — operator choice meaningfully affects measured importance. 2405.20404 VERIFIED but landscape MIS-IDs it — it is JoPA: Joint Prompt Attribution (Chang et al., ACL 2025), NOT "XPrompt"; combinatorial-effects claim survives; doctor/patient example not in abstract (report embellishment). 2409.00729 (ContextCite, NeurIPS 2024) VERIFIED — fits LASSO sparse-linear surrogate over random-subset ablations, deliberately NOT one-at-a-time LOO. 2312.15395 (Liu, Shapley prompt valuation) VERIFIED title; "Shapley fixes LOO additivity" is INFERENCE from Shapley theory not abstract quote. 2510.09738 (Han, Judge's Verdict) VERIFIED but report OVER-CLAIMS — it is judge-capability/human-agreement classification, does NOT prescribe "refuse uncalibrated judges"; refuse-discipline rests on llm-judge-calibration + independence theory, NOT this paper.

EVR-1 / LOO is field's inferior estimator; frame Contribution as lower bound. MAJOR. Anchor PRD headline / Track D delta semantics. Single-clause ablation IS n=1 LOO; ContextCite rejects it for random-subset surrogate; Shapley-for-prompts exists because LOO violates additivity/efficiency (INFERENCE). Rec: do NOT relabel metric; report column "Contribution (LOO, n=1 Shapley special case)" + state "magnitude is lower bound under redundancy; absence of delta is not absence of contribution". Random-subset surrogate (LASSO over masks, ~2-4m fitted ablations NOT 2^m, ~2-4× call cost) = credible v0.2. Disposition TRACK-D-EXIT-CRITERION (honest-framing string is v0.1 ship-blocker on reports) + DEFER-V0.2 (surrogate engine).

EVR-2 / mandate versioned neutral substitution operator. BLOCKER. Anchor cli/main.py:131-134 (VERIFIED: raises not-implemented). 2409.09951 shows operator choice swings importance; deletion conflates clause-effect with coherence/length/format; repo has ZERO operator policy. Rec: implement prose analogue of resample/optimal — matched-length semantically-null placeholder, NOT deletion; versioned artifact like Tier-1 metric, store ablation_operator_id + implementation_hash on every verdict (mirror metric_versions). A11 confound monitoring meaningless without this — deletion injects length confound on EVERY axis. Disposition ADOPT-AS-A39 + TRACK-D-EXIT-CRITERION (no verdict writes without ablation_operator_id).

EVR-3 / redundancy cancellation real; FLAGGED_CONFOUNDED structurally blind. MAJOR. Anchor A11 / UNMEASURED. JoPA (2405.20404) establishes prose components have combinatorial effects LOO misses; two redundant clauses each ~0 solo delta → UNMEASURED despite jointly load-bearing; A11 guards cross-axis movement not within-axis cancellation. Rec: paired-ablation probe TRIGGERED not blanket — for clause returning UNMEASURED(no_data) with a claimed axis, remove pair {N, sibling-on-same-axis}, compare to sum-of-singles, superadditive gap ⇒ reclassify UNMEASURED→CONFOUNDED(redundant_pair). Blanket O(m²) budget non-starter. Disposition DOCUMENTED-LIMITATION v0.1 (write into reports + PRD §11) + triggered probe TRACK-D-EXIT-CRITERION if budget headroom else DEFER-V0.2. STAT co-owns superadditivity threshold.

EVR-4 / operator-self-label: REFUSE holds. BLOCKER (to any flip). Anchor §C2 / A34. Calibrating judge against operator-who-owns-skills-under-test collapses independence assumption that makes κ/agreement mean anything. Defect NOT κ≈1.0 (distinct raters, already corrected) — real defect: self-labeled set certifies only "agrees with this operator", never axis-level; that IS the cross-axis-inheritance prohibition restated. Authority = llm-judge-calibration Disciplines 3-6 + independence, NOT 2510.09738 (classifies capability, doesn't license self-labeling). Flip's guardrail (pool rejects self-labeled) concedes non-evidence-grade → admitting as a "tier" is theater. Rec: HOLD REFUSE v0.1, no operator_self_labeled state. If future fire flips (should NOT for an adversarial-audit product whose value is third-party independence — see EVR-6): provisional_non_admissible, hard-excluded from pool at VIEW layer (not app layer), 30-day cadence, every report stamped [operator-calibrated — not independently validated]. Disposition VALUE-DECISION rendered as SME verdict → REFUSE; do not surface to user; record as C2 resolved = REFUSE.

EVR-5 / A29 JOIN directionality CORRECT; do not invert; one gap. MINOR. Anchor migrations/evidence/0003:29-35 (VERIFIED). VIEW excludes verdict v when confound_events row has primary_clause_id=v.clause_id AND delta_kind='confound_flagged'. Exclusion question = "exclude verdicts where THIS clause's ablation was confounded"; primary_clause_id = ablated clause = clause being judged → matching v.clause_id=ce.primary_clause_id is CORRECT, NOT inverted. Open sub-question (should affected clause also be tainted?): NO, not at admissibility VIEW — affected clause moved on an unclaimed axis during N's ablation = observed_unclaimed_delta audit row (A11), not grounds to exclude affected clause's OWN-ablation verdicts; tainting would over-exclude + shrink coverage. Gap: confirm confound_events records the ABLATED clause in primary_clause_id at write time (Track D's job) — if runner writes affected clause there, JOIN inverts. Add write-side assertion. Disposition ADOPT — directionality confirmed correct, A29 closes + TRACK-D-EXIT-CRITERION (assertion primary_clause_id==ablated_clause_id).

EVR-6 / positioning — reframe claim BOTH novel AND defensible. OBSERVATION. "no twin" high-confidence but headline method is field's inferior estimator → measurement-theory reviewer lands undermining blow if we claim "ablation measures contribution". Rec: defensible novel claim is NOT the estimator — it's the evidentiary discipline around it: "falsifiable directional contracts with write-time admissibility-gated provenance" (the P4 signature, zero hits in eval domain per landscape §1). LOO honestly positioned as CONSERVATIVE estimator inside that discipline — under-crediting is a SAFE failure for adversarial audit (false-negative on contribution ≠ false-positive on a pass). Pitch: "we don't claim to fairly attribute; we claim every PASS is backed by admissible, provenance-stamped, directional evidence — and refuse to claim anything else." Disposition ADOPT into PRD §1 reframe (v1.1) — replaces D2 "manufactured primitives" edit.

Cross-talk: STAT right on superadditivity threshold for EVR-3 probe + Beta-Binomial impact of EVR-2 (length-confounded delta inflates variance, widens every posterior, N_inc burns budget chasing phantom); may under-weight EVR-1 framing as "just documentation" — it's a stopping-rule honesty issue: lower-bound metric means stop_fail can fire on redundant-but-load-bearing clause = false FAIL not just false UNMEASURED; co-own C2 pool-exclusion mechanics. COST right on cache math but WRONG to push C2-flip on ships-testable (self-labeling makes v0.1 falsely testable, tokens spent on inadmissible verdicts = A17 trap); price v0.2 surrogate at ~2-4m calls not 2^m (Shapley exponential is the strawman making LOO look necessary). RELIABILITY under-weight: EVR-2 ablation_operator_id must snapshot at verdict write-time (A3); operator change mid-run + crash-resume picking new version silently mixes two operators in one condition-pair — resume must pin operator-version to run not re-resolve. OPERATOR-DX right dry-run must surface operator + lower-bound caveat; would MISS that paired-ablation probe is a SECOND user-visible long-running op with its own budget line, can't be silent. status: nominal.
